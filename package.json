{
  "name": "vault-mcp",
  "version": "0.5.0",
  "description": "Local knowledge base MCP server for Cursor and VS Code with Markdown, academic papers and code search",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "vault-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "npm run typecheck && node --import tsx --test tests/*.test.ts",
    "test:integration": "npm run build && node --import tsx --test tests/integration/*.test.ts"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.0",
    "@xenova/transformers": "^2.17.2",
    "better-sqlite3": "^13.0.3",
    "chokidar": "^5.0.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^22.13.0",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
